
import 'package:flutter/material.dart';

class DetalhesReserva extends StatelessWidget {
  const DetalhesReserva({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Detalhes da reserva')),
      body: Column(
        children: [

        ],
      ),
    );
  }
}