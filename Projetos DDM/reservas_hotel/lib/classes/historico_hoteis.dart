import 'package:flutter/material.dart';

class HistoricoHoteis extends StatelessWidget {
  const HistoricoHoteis({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Histórico de Hoteis')),
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('Ainda não sei como colocar uma parte já preenchida'),
        ],
      ),
    );
  }
}