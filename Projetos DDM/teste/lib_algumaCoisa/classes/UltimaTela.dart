import 'package:flutter/material.dart';

class UltimaTela extends StatelessWidget{
  const UltimaTela({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parabéns, você chegou na última tela finalmente'))
    );
  }

}