import 'package:flutter/material.dart';

class QuintaTela extends StatelessWidget{
  const QuintaTela({super.key});
  
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('Última tela')
      ),
    );
  }
}